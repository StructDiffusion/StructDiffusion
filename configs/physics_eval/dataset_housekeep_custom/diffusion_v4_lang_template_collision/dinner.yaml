random_seed: ${base.random_seed}
structure_type: dinner
generator_model_dir: ${base.models.diffusion_lang_template}
data_split: train
data_root: ${base.dirs.experiment}/diffusion_lang_template_dinner_collision
collision_model_dir: ${base.models.collision}
collision_score_weight: 1.0
discriminator_score_weight: 0.0
num_samples: ${base.rejection.num_samples}
num_elite: 1
discriminator_inference_batch_size: 24
assets_path: ${base.assets_path}
object_model_dir: ${base.object_model_dir}
redirect_stdout: ${base.redirect_stdout}
shuffle: False
max_num_eval: ${base.max_num_eval}
visualize: ${base.visualize}
override_data_dirs:
  - ${base.dirs.data}/dinner/result
override_index_dirs:
  - index